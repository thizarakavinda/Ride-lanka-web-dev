import "./admin.css";

export const metadata = {
  title: "Admin · Ride Lanka",
  description: "Ride Lanka administrator dashboard",
};

export default function AdminLayout({ children }) {
  return children;
}
